import React from "react";

type SmallCardProps = {
    title: string,
    description: string,
    price: string,
    imgUrl: string
}

function SmallCard({title, description, price, imgUrl}:SmallCardProps) : JSX.Element {
    return(
        <div>

        </div>
    )
}

export default SmallCard